---
title: Colors
description: Generated colors
---

<% _.each(allProperties, function(prop) { %>

 {{< swatch color="<%= prop.name %>" value="<%= prop.value %>" comment="<%= (prop.comment) ? prop.comment : ''%>" >}}

<% }); %>

